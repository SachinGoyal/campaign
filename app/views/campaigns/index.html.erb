<div class="row">
  <div class="page-title">
    <div class="title_left"><h2>Campaign List</h2></div><!-- title_left -->
      <div class="title_right">

      <div class="col-md-9 col-sm-12 col-xs-12 form-group pull-right top_search">
          <% if can? :search, Campaign %>
            <%= render 'shared/keyword_search', :condition => :name_cont %>
            <div class="advance-search">
              <%= link_to search_campaigns_path do %>
              <i class="fa fa-dot-circle-o"></i>
              <% end %>
            </div> 
          <% end %>
          <% if can? :create, Campaign %>
            <div class="custom-add"><%= link_to new_campaign_path, :title => "New Campaign" do %><i class="fa fa-plus"></i><% end %></div>
          <% end %>

      </div>

    </div>
      <div class="clearfix"></div>                
  </div><!-- page-title -->
</div><!--row-->
<%= render 'results'%>
<%= will_paginate @campaigns , renderer: BootstrapPagination::Rails %>